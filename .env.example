# Copy to .env.local for Vercel CLI or set in Vercel Dashboard → Settings → Environment Variables

DOWNLOAD_PASSWORD=your_secure_password
# Optional: custom writable directory (defaults to /tmp/acko-data on Vercel)
# DATA_DIR=/tmp/acko-data
# CORS_ORIGINS=https://your-domain.vercel.app,http://localhost:3000
